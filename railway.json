{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "railway/Dockerfile.web"
    },
    "deploy": {
        "//": "Health must not depend on the room service. Checking the proxied path would fail the very first deploy, before that service exists, and would later mark this service unhealthy whenever the room service restarts. Use /watch-party/healthz manually to test the whole chain.",
        "healthcheckPath": "/",
        "healthcheckTimeout": 60,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
